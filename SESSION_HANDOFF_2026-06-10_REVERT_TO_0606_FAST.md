# 引継ぎ書 2026-06-10 深夜 — 「光りが遅い」は06-06以降のコード回帰と判明 → 06-06 engineに戻して高速復活

> **これは何**: 数セッションかけて追っていた「NOW後にGUIが光る(決済)のが遅い／追従が間に合わない」問題が、
> **2026-06-06以降に入れたコード変更による回帰**だと最終確定し、**06-06版engine(commit `1c3e3a7`)に戻したら光りが激速に復活**した記録。
> ユーザの「一度すべて正常に動いていた、矛盾している」という指摘が突破口。**大規模改修は不要**だった。
> 対象機: bafather (162.43.83.54) / 稼働install = `C:\BACOPYRECEIVER_user01\`

---

## 0. いま誰かがこれを読むなら（結論）

- **bafather は今 06-06版 engine `68696844`（= commit `1c3e3a7` の bot/executor/money/match 4ファイルでビルド）で稼働中。光りが激速・追従正常・NOW→BET正常・スクロール正常**（ユーザ実機確認済 02:5x「めちゃくちゃ早い」）。
- **「光りが遅い」の真因 = 06-06より後のコミットが入れた回帰**。環境(Chrome/マシン)要因でも、48卓だから構造的に遅いのでもない（←これは前Claudeの誤った結論）。
- **最有力の犯人 = SEQ-Supabase永続化(`fa66b96`/`95ce0ab`)**。メモリにも「決済/追従が遅延する回帰」と既に記録あり。
- **ただし06-06版は、その後の"良い修正/機能"も巻き戻している**（下記§3）。**それらの再実装が必要**。

---

## 1. 現在のデプロイ状態（bafather）

| 物 | 値 | 備考 |
|---|---|---|
| **engine** | **`68696844`** = commit `1c3e3a7`(06-06) の `dual_line_pragmatic_bot.py`/`dual_line_live_executor.py`/`dual_line_money.py`/`dual_line_match.py` でビルド | **速い・正常** |
| asar | `234275`（ログローテーション入り・06-09の `9379162` 由来） | **engineと別系統なので維持されている**＝ログ肥大の心配なし |
| .env | クリーン（当セッションで足した `BACOPY_WS_DEFER_CENTER`/`FAST_LPBET_CONFIRM`/`*_RECENTER_SEC` は削除済） | auto-followモード等の本来envはGUI/main.js管理 |
| backup engine | `68698219`（直前の版・`_baf_swapeng.ps1` の `.bak_<stamp>`） | 即ロールバック可 |

- ビルド元ファイルの抽出: ローカル `E:\dev\Cusor\bacopy\_rev0606\`（`git show 1c3e3a7:<file>` で取り出した4ファイル）。
- 1c3e3a7 の bot/executor は新規 `dual_line_logic.py`(06-07以降追加) を **import しない**ので、4ファイルだけで完結ビルドできる（確認済）。
- ビルド/swap: bafather `C:\bacopy\` に4ファイルscp → `_build_engine_handend.ps1` → `_baf_swapeng.ps1`。env掃除は `_baf_resetenv.ps1`（ローカルにあり、要scp）。

---

## 2. 経緯（迷子になった過程＝同じ轍を踏まないために）

**症状**: 「NOWでBETはするが、勝敗結果がGUIに光るのが遅い。テレコ追従は1手遅れて逆張りになり負ける」。

**前Claude(私)の迷走**:
1. 最初に「ログ肥大2.29GB」を直した（main.jsローテ=asar 234275）。系統的ズレは改善したが、光りの遅さは残存。
2. 計装を入れて切り分け: dga勝者は hand終了の**5秒前**に届く＝**フィードは速い(無罪)**。遅いのは**決済(pump)がメインループでstarve**。
3. さらに計装: メインループを最大**42秒ブロック**しているのは `executor.tick()`（センタリングの`frame.evaluate`が48卓の重いDOMで固まる）と特定。
4. ここで私は **「48卓だから構造的に重い→WS処理をDOMから分離する大規模改修が必要」と誤結論**。env(再センタリング頻度↓)も試したが7-14秒までしか改善せず追従に間に合わない。
5. **ユーザが指摘**: 「**過去に一度すべて正常に動いていた日がある（自動化が完成した日）。その後あちこち触って壊れた。大規模改修が必要というのは矛盾している**」。
6. → MDファイル `DUAL_LINE_AUTO_WS_BET_COMPLETE_2026-06-06.md` を発見＝**2026-06-06が「全自動完成・全部正常」の日**。commit `1c3e3a7`。
7. → 06-06版engineをビルドして実機投入 → **光りが激速に復活**。**回帰確定・大規模改修不要**と決着。

**教訓（重要）**:
- ★「一度正常だった」なら、構造論より先に**回帰を疑い、正常コミットに戻して二分探索**する。これが最短。
- ★前Claudeは本番機(bafather)で計装版/実験版を投入しまくり、ユーザに「パターン誤表示・ベット暴走」の混乱と恐怖を与えた。**本番で実験しない／壊したら即リバート**。
- ★`executor.tick`が42秒固まる事実は本物。だが**それは回帰で悪化した**のであって、06-06では同じtick/センタリングでも速かった（＝tick内で重くなった原因が回帰コミットにある）。

---

## 3. ★06-06に戻したことで「失われた/巻き戻った」変更（再実装が必要）

> 06-06版engine = `1c3e3a7` の4ファイル。**06-07以降に入れた以下は全て engine から消えている**。
> **良い修正・欲しい機能は再実装が必要**。一方 SEQ-Supabase は回帰の容疑なので「直して」入れる。

### 3-A. 再実装したい「良い修正/機能」（速度回帰とは無関係の純バグ修正が濃厚）
| commit | 日 | 内容 | 重要度 |
|---|---|---|---|
| `0e0c5ee` | 06-07 | **追従チェーンdesync修正**（stale re-bet → 181秒ブロックを解消・re-betを確実に着弾） executor+bot | ★高（追従の信頼性） |
| `53144e3` | 06-07 | **二重decision孤児化の修正**（同一decision二重配信で約定済BETが孤児化→決済不能/追従不発を解消） bot | ★高（追従/決済の信頼性） |
| `fa66b96`(一部) | 06-08 | **HOLDスクロール修正** ＋ **SEQ recovery floor**（回収中$1戻り解消・overshoot>0→next_idx≥1） executor+money | 中（HOLD=手動user・SEQ=資金） |
| `1d323f2` | 06-09 | **HOLD freeze 全センタリング網羅**（HOLD押下時に黄色枠でスクロールされる不具合を `_center_multi_tile` 先頭ガードで解消）※今日のuser02手動の不具合修正 | 中（手動user02向け） |
| `9379162`(一部) | 06-09 | **決済時 NOW-LOCK 即解除**（赤/青オーバーレイを決済時に消す） bot | 低〜中 |
| `3e72e64` | 06-07 | **pline/bline 再定義(5+/≤1-opposite/T無視) + v4 `bline\|telecho\|B` swap** match.py | 要判断（v4パターン定義・[[project_dual_line_telecho_3col_no_edge_2026-06-08]]関連） |

### 3-B. 回帰の容疑（直してから入れる・そのまま入れない）
| commit | 日 | 内容 | 扱い |
|---|---|---|---|
| `fa66b96`(本体) | 06-08 | **SEQ-Supabase永続化**（課金POSTに`dual_seq`相乗り＋起動時GET復元）。**決済ホットパスに per-settlement の処理が増えた疑い** | ★**最有力の回帰犯**。timing安全な形(完全別スレッド/決済経路に触れない)でしか入れない |
| `95ce0ab` | 06-09 | SEQ-Supabase を専用スレッドに分離(WIP) | 同上（これでも遅延が残ったとの記録＝設計見直し要） |

> **失う機能の実害**: SEQ-Supabaseが無いと「プロファイル強制リセット/破損時に深いSEQを失う」復元ができない。ただし**6時間定期再起動のSEQ消し対策・クラッシュ復旧はasar側で生存**。失うのは"最終復元"のみ（メモリ [[project_dual_seq_supabase_persistence_2026-06-08]]）。

### 3-C. 今日の実験的変更（回帰を直そうとして入れた物・回帰本体を消せば不要の可能性大）
| commit | 内容 | 扱い |
|---|---|---|
| `c6387b7` | DOM-center deferral（WS時センタリング後回し） | 回帰の対症療法。本体除去後は不要かも。ユーザは「NOWスクロールしない」不具合を体験→入れ直すなら要検証 |
| `e1aca4d` | lpbet_fast（自分のlpbet観測で即確証） | 対症療法。06-06が速いなら不要 |
| `27cd1db` | game_id確証（`game=-`で空振り＝無効） | 不要 |

### 3-D. 巻き戻っていない（asar側なので生存）
- **ログローテーション(50MB)**: asar 234275 に存在＝**生きている**。engineを06-06に戻してもログ肥大しない。

---

## 4. これからの完成への道筋（推奨手順）

1. **今**: 06-06 engine(`68696844`)で安定運用。ユーザは速い状態を取り戻した。
2. **犯人の最終特定（二分探索）**: 「06-06 ＋ `0e0c5ee` ＋ `53144e3`」をビルドして速度確認 → なお速ければ、`fa66b96`/`95ce0ab`(SEQ-Supabase)が犯人で確定。
   - 速ければ**これが理想の土台**（速い＋追従desync/孤児化の良修正入り）。
3. **良い修正の足し戻し**（§3-A）を1つずつ・実機速度を確認しながら。**SEQ-Supabase(§3-B)は決済ホットパスに一切触れない形でのみ**。
4. **3名に配布**: 理想版が固まったら user02/03/04 にインストーラ再ビルドで配る（全員同じ回帰で遅いはず）。
   - 注: user02 は手動運用→ §3-A の HOLD修正(`1d323f2`/`fa66b96`一部)が効くと嬉しい。
5. **検証の物差し**: hand-end→「光り」が**数秒以下**か。追従(テレコ/ドラゴン)が**次の窓に間に合う**か。実機目視が最優先。

---

## 4.5 ★残課題: 間欠的な「黄色枠つきっぱなし→消えると光る」（06-06でも残る別問題）

- **症状(ユーザ証言 2026-06-10 03:xx)**: 06-06 engineでも**たまに**、NOW後に**黄色枠(オーバーレイ)が消えずに残り、その枠が消えた瞬間にGUIが光る**(決済表示)。**多くのBETは速い**(あなた「めちゃ早い時はこれが起きてない」)。**リバート前にもあった＝回帰とは別の元からの間欠問題**。
- **切り分け**: 「毎回遅い」=回帰(06-06で除去済)。「たまに黄色枠stuck」=**別の間欠問題で残存**。
- **有力仮説**: 「**黄色枠が消える＝光る**」という証言から、**枠消しのDOM操作(`clear_manual_assist_overlay`=frame.evaluate)が、たまにtickの重いブロック(48卓センタリングframe.evaluate)に巻き込まれて遅延** → 枠が消えるのが遅れ、その瞬間に決済表示が出る。**お金の決済自体は済んでいて、画面表示(枠消し+光り)だけ間欠的に遅れる**可能性。
- **調査の入口**: ①`clear_manual_assist_overlay`の呼出と実行の時刻差を計測 ②黄色枠stuck時にexecutor.tickが長かったか(LOOP-SLOW計装を06-06ベースに足して相関を見る) ③`_maintain_assist_focus_hold`の予告overlay再スタンプ/TTL不発(メモリ「黄色枠stuck 2026-05-30修正」の取りこぼし) を疑う。
- **位置づけ**: 「毎回遅い」が直った今、これは**最後の磨き込み**。急がず専用で。

## 4.6 ★間欠yellow-stuckの正体＝48卓betting Chromeのフリーズ（2026-06-10 追検証で確定）

**追検証の結論**（`53144e3`+LOOP-SLOW計装エンジン `68698192` で実測）:
- **遅さは2層**だった。①**系統的(毎回)＝SEQ-Supabase回帰** → §3で除去済(06-06/53144e3)。②**間欠(たまにstuck)＝別物で残存**。
- 間欠の正体: `executor.tick()` が **5〜9回/分・2〜13秒(起動直後は最大141秒)ブロック**。**centeringではない**(再センタリングを30sに削減しても頻度・最大とも不変)。**Chrome経時劣化でもない**(起動12分の新品Chromeでも141sフリーズ)。
- **真因=48卓のbetting Chrome(:9222)がレンダリング負荷で時々フリーズ**し、tick内のDOM操作がそこで固まる→決済pumpがstarve→黄色枠stuck。**全バージョン共通の構造的問題**(原06-06にも存在・系統遅延が無いので速く感じていただけ)。Chrome合計RAM~1.9GB/レンダラ2本が~700MBずつ。
- **検証ログ**: `_loopslow53.txt`(53144e3のtick-block 146回/28分・最大22.5s)、`_tick30.txt`(recenter=30でも5.9/分・max141s)、`_baf_chromeage.ps1`(新品Chromeでもフリーズ)。
- **本当に消すには(構造的・別プロジェクト)**: ①**決済を専用スレッドに分離**(tickのChromeフリーズに巻き込まれない・但しmoney model/executor stateのロック設計が要・WS送信のPlaywright依存も要確認) ②**betting Chromeの卓数を減らす**(48→必要分)でフリーズ自体を軽減。
- **当面**: `53144e3`(68698192)が実用上の良い土台。間欠stuckは48卓の宿命として受容(ほぼ速い・たまに2-13s)。recenter env(30s)は無効果なので次回再起動でデフォルトに戻してよい(`_baf_resetenv.ps1`)。

## 4.7 ★★間欠yellow/red-stuck・追従ミスの真因＝「同一卓への繰り返し卓切替freeze」→ skip-refocixで解決 (2026-06-10)

**§4.6で「48卓フリーズ」とした間欠stuckの、さらに深い真因を特定・修正した（本丸）。**
- **tick細分計測**(engine 68696193に `[TICK-SLOW]` 追加: `_ensure_multi_area`/`_inject_all`/`_perform_switch`/`_find_pragmatic_frame`/probe.evaluate)で実測 → **フリーズの圧倒的主因 = `_perform_switch`(卓切替のFOCUS DOM操作) intent=preposition、各5-19秒**。
- **メカニズム**: 予告(forecast)はscore更新(3→2→1)で同じ卓へ何度もVPSから来る → botが**同一卓へ予告切替(FOCUS=48卓グリッドからタイル探索+click)を繰り返す** → 各5-19秒freeze → 決済pump/追従発注/NOWが全部ブロック → 赤/青枠が決済まで残る(stuck)・追従が窓に間に合わず逆張り爆死。実例(10:51): 同卓へ4回切替+BET後2回、BET→決済が**47秒**。
- **なぜbafatherだけ**: user02(同コード・同オート)はFOCUSが速く問題化しない。bafatherのbetting ChromeはFOCUS(render thread)が遅く、繰り返しで増幅。CPU/Defender/centeringは無関係(全部検証で否定済)。
- **修正(engine 68699438)**: `_perform_switch` 冒頭に**「既に準備済みの卓(`_prepared_table_id == target`)への予告/manual_assist再切替はFOCUSスキップ」**ガード追加(env `BACOPY_SKIP_REFOCUS_PREPARED`=1既定)。WS BETにFOCUSは不要なので安全(`_start_assist_focus_hold`で中央保持だけ継続)。
- **実証**: skip re-focus 6-20回/窓発火・実FOCUS 1-5回のみ・**TICK-SLOW _perform_switch 17→1-5件(70-85%減)**。ユーザ実機「赤枠で追従出来てタイミングも良い」(2026-06-10 11:2x)。
- **残**: 新卓への"初回"FOCUS(各6-16s)は残る(避けられない)。さらに詰めるなら**WSモードでは予告FOCUS自体を省く**(WS BETにFOCUS不要・視覚は犠牲)。FOCUS失敗卓(例ne074)は準備済みにならず再試行freezeが残る→時間throttle追加が次善。
- **engine 68699438 = 53144e3 + skip-refocus + tick計装(LOOP-SLOW/TICK-SLOW、低頻度warningで無害)**。配布時はこのまま or 計装抜きクリーン版を。

## 5. ロールバック/操作メモ
- 06-06版に問題が出たら即: bafather `C:\bacopy\bacopy_engine.exe.bak_<stamp>`(=68698219) を戻すか、`_rev0606` を使わず現行HEADを再ビルド。
- engine swap は GUI停止→`_baf_swapeng.ps1`→ユーザがRDPでGUI再起動（session0からChrome不可視で起動不可）。
- SSH越し inline PowerShell は `$_`/`$e` が bash に食われる→**必ず.ps1をscpして`-File`実行**。
- ローカル作業物: `_rev0606/`(06-06の4ファイル)、`_baf_resetenv.ps1`(env掃除)、`_baf_recenter_chk.ps1`(光り計測・※06-06はマーカー形式差で空表示あり=目視優先)。

---

## 6. 関連
- 完成記録: `DUAL_LINE_AUTO_WS_BET_COMPLETE_2026-06-06.md`（06-06の全自動WS BET仕様の本体・トラブルシュート）。
- メモリ: [[project_dual_seq_supabase_persistence_2026-06-08]]（SEQ-Supabase回帰の記録）、[[project_dual_line_log_bloat_follow_timing_2026-06-09]]（ログ肥大の件）。
- 前引継ぎ: `SESSION_HANDOFF_2026-06-09_FOLLOW_TIMING_RESOLVED.md`（※「解決」は誤り・本書が訂正版）。
