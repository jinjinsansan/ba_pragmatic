# NOW密度フィルタ 事前登録 (2026-07-19 凍結)

ユーザー仮説(2026-07-19): 「NOWは時間的に固まって出る時間帯と過疎の時間帯がある。過疎の時、ようやく来たNOWはハズレが多い」。
→ 検証で**向きが逆**と判明: **密集NOWの方がハズレが多く、過疎(孤立)NOWの方が当たる**。v4はOOSを生き残り過疎群が手数料分岐を超えた。単体z<2のため実装せず事前登録して前向き検証する。

**この文書の目的**: 基準を結果を見る前に凍結し、多重比較・後知恵・OOS死を防ぐ。カードR1/R2・R-DOWと同じ規律。

---

## 発見の経緯(凍結前データ = 5/30〜7/19・v4 settled 19,558件)

密度=各NOWの直前T分に出たフリートNOW本数。全て直前情報のみ(look-ahead厳禁)。

| 検定 | 結果 |
|---|---|
| フリート全体d30(決済時刻)v1 | v3/v4・d30/d60/gap **全セルで dense<sparse** 符号一致・時刻層別後も残存(v4 -1.64pt/v3 -1.52pt) |
| OOS(決済時刻)v2 | v4: OOS +3.03pt(sparse 52.17%・z=1.79)・パターン交絡除去後+1.62pt / **v3はOOSで消滅**(+1.66→-0.07) |
| 到着(publish)時刻 v3 | v4 全履歴 sparse 50.85% vs dense 49.69%(+1.16pt z=1.29)・**OOS +2.24pt**(sparse 52.06% n=1625) |

- 好不調レジーム(直前勝率)はシャッフル対照z<1.1・符号がtrain↔OOS反転=ノイズ(却下)。それと違いNOW密度は**符号が一貫**し交絡に頑健。今日の全レジーム探索で唯一OOSを越えた候補
- スクリプト: `_bt_now_density.py`(v1) / `_bt_now_density_v2.py`(OOS+パターン) / `_bt_now_density_v3_arrival.py`(到着ベース・cut確定)

---

## ★凍結定義(変更禁止)

- **系統**: v4 のみ(v3はOOSで消滅=参考扱い)
- **密度母集団**: フリート全体NOW到着 = v4 `[v4-publish]` 時刻 + v3 `resolve` 時刻(v3は到着行が無いため決済時刻で代用・約1手ラグ・30分窓に対し軽微)
- **d30**: 現v4 NOWの**到着(publish)時刻** t の直前30分 `[t-1800, t)` に出たフリートNOW本数(自分除く)
- **cut(凍結)**: `sparse: d30 < 11` / `dense: d30 >= 16` / mid(11〜15)は主判定から除外
- **対象**: `publish_ts > 1784396816`(= 2026-07-19 02:46:56)の v4 NOW・TIE除外・順張り的中率
- **凍結epoch**: `1784396816`

## 合格条件(前向きデータで全て満たす)

| # | 条件 | 意味 |
|---|---|---|
| a | sparse − dense ≥ **+2.0pt** | 密度差が実体を持つ |
| b | sparse 的中率 > **50.8%** | 過疎群が手数料分岐を超える(=張る価値) |
| c | z(sparse vs dense) ≥ **2.0** | 単体で有意 |
| d | sparse n ≥ **1,500** | 約2〜3週間(過疎は約100件/日)→**判定は8月上旬頃** |

- 中間値では**判定も実装もしない**(基準を動かした時点で事前登録が無効化)
- 合格時の実装形 = **BET/skipフィルタ**(方向スイッチではない): v4のNOWで d30<11(過疎)なら張る / d30≥16(密集)ならスキップまたは減額
- 不合格なら破棄(過去の示唆同様OOS死の可能性を最初から織り込む)

## 監視

```
ssh -i ~/.ssh/laplace_vps root@210.131.215.116 "python3 /opt/laplace2/_now_density_prereg_watch.py"
```
ユーザー「NOW密度チェックして」で実行。sparse/dense的中率・4条件の進捗・sparse n進捗を出力。

## 留意(鵜呑み禁止)

- z=1.3〜1.8=単体有意未満・単一系統(v4のみ)・単一ホールドアウト。**v3がOOSで消えた**のは警告
- d30/d60/gapは互いに相関=独立確認ではない。前向きOOSが唯一の本判定
- 卓構成の交絡は未統制(前向きOOSが自動的に吸収する想定)
- 合格しても実装前にさらに1〜2週間forward推奨(カードR1と同じ慎重さ)
