# 3方向同時復活 — 開発計画 (2026-09-12)

> 2026-08-22 に全停止したプロジェクトを、**3つの異なる要望**に応える形で復活させる。
>
> - ① **梶原チーム(日本)** … 元通りの全部。テレグラム4ch + GUI + SEQ。★ただし `stake.com` は日本から 451 なので**ミラードメイン**を使う必要がある
> - ② **韓国チーム** … 梶原ロジックを使いたい。★**技術を一切盗ませない**。韓国語UI・テレグラム無し・専門用語の表示無し
> - ③ **田辺チーム(日本)** … V2(マスター画面 → 受け子GUI 連動)の半手動。SEQ・パターン不要。★①と同じミラードメイン
>
> 前提: `RESTART_KIT_2026-08-22.md`(停止の全体像)、`MIRROR_REVIVAL_PLAN_2026-08-28.md`(③の下調べ)。

---

## 0. TL;DR — 5行

1. **3製品ではなく、2軸の組み合わせ。** 「頭脳がどこで動くか(クライアント/サーバー)」×「誰が決めるか(アルゴリズム/人間)」。②と③は**同じ配管**に乗る。
2. **②の答えは「暗号化」ではなく「そもそも渡さない」。** exe の難読化はエンジニア相手に数時間しか持たない。ロジックをサーバーに置けば、盗む対象がクライアントに存在しなくなる。
3. **それが安く済む決定的な理由**: 梶原ロジックの本体 `decide(history, next_n)` は **344行・stdlib のみ・I/O ゼロの純関数**(本日実測)。サーバーへ移すのに書き直しが要らない。
4. **★9/12 実測: `stake.com` は 451 で日本から死亡。ミラー121本は生存(403=Cloudflareのbot判定)。dgaフィードは無傷。**
   ただし「非CDN URL」ではなく「**まだブロックされていないCloudflareドメイン**」。**焼かれる度にローテーションが要る**ので、
   オリジンは exe に焼かず**サーバーが配って自動フェイルオーバー**する設計にする。
5. 着手順は **Phase 0(URL検証) → 共通基盤 → ③(最小・実証済) → ①(復元) → ②(新規)**。

---

## 1. 3要望を2軸に分解する

|  | 決定するのは**アルゴリズム** | 決定するのは**人間** |
|---|---|---|
| **クライアント側で計算** | **① 梶原チーム**<br>`dual_line` モード(現行) | — |
| **サーバー側で計算** | **② 韓国チーム**<br>★新規だが配管は③と同じ | **③ 田辺チーム**<br>`executor-pragmatic` モード(現行) |

受け子アプリは既に**1本で2モード**を持っている(`copytrade_gui/src/main.js:989` が
`dual-line` / `executor-pragmatic` を出し分け)。つまり:

- ③ = 既存の `executor-pragmatic` + マスター画面。**8/28にローカル一周実証済み**
- ② = ③の配管の「人間の指」の部分を、**サーバー上で回る `decide()` に差し替えるだけ**
- ① = 既存の `dual_line`。**触らずに復元するのが最も安全**

→ **②は新規製品ではなく、③の派生。** ここが今回の設計上の最大の節約点。

---

## 2. ★中核の発見 — 梶原ロジックは「移せる形」で書かれている

本日の実測:

| ファイル | 行数 | import | I/O | 性質 |
|---|---|---|---|---|
| `dual_line_logic.py` | 344 | `dataclasses` `typing` **のみ** | **ゼロ** | `decide(history:str, next_n:int) -> Decision` の**純関数** |
| `dual_line_match.py` | 158 | 上記 + stdlib のみ | **ゼロ** | 同上のラッパ |
| `dual_line_money.py` | 367 | + `json/os/Path` + `marubatsu_strategy` | 状態JSON | SEQ。**状態を持つ**がDBに移せる |

```
検証コマンド: grep -c "playwright|requests|websocket|os.getenv|open(" dual_line_logic.py → 0
```

**意味すること**:

- 6パターン/10パターンの頭脳は **869行に収まっている**。ブラウザも通信も要らない。
  結果履歴の文字列(`"BPPBTB..."`)を渡すだけで動く
- だから **サーバー側で回すのは移植ではなく、ただの配置換え**。工数が小さい
- 同時に、**これは「盗まれたら一瞬で終わる」という意味でもある**。869行のテキストファイルが
  この事業の全資産。クライアントPCに置いてはいけない

`Decision` が持つフィールド(そのまま漏洩源になる):

```python
china_pattern: 'telecho'|'niconico'|'sansan'|'pline'|'bline'   # 珠盤路 5種
big_pattern  : 'dragon'|'telecho'|'niconico'|'nikoichi'        # 大路 4種
```

→ **韓国向けの通信payloadとGUIには、この文字列を絶対に載せない。**

---

## 3. ②「盗ませない」の正直な設計

### 3-1. 効かない対策(やっても意味が薄い)

| 手段 | 実際の耐性 |
|---|---|
| `app.asar` のパッケージ化 | **ゼロ**。`npx asar extract` で展開。暗号化ではなく tar と同じ |
| PyInstaller `--onefile` | **数十分**。`pyinstxtractor` → `.pyc` → デコンパイルは定番手順 |
| PyArmor 等の商用難読化 | **数日〜数週間**。時間稼ぎにはなるが、エンジニアがいる相手には期限付き |
| GUI から用語を消す | 表示は消えるが**バイナリ内の文字列は残る**。必要だが単独では不十分 |

★相手チームに**エンジニアがいると分かっている**以上、「クライアントに置いて隠す」は成立しない。

### 3-2. 効く対策 — ロジックをクライアントに置かない

```
[韓国チームの受け子PC]                    [オーナーのサーバー]
  受け子GUI (韓国語・機能削減版)
  engine = 実行専用                        dga直結フィード
     ├ 卓へ移動する                    →   決定エンジン brain.py
     ├ 指示された側に BET する               └ decide() ← ★ここにだけ存在
     └ 結果を返す                          SEQ 状態(executor別・DB)
        ↑                                        │
        └────── {table, side, amount} ───────────┘
                   これしか流れない
```

**渡すのは「どの卓の、どちら側に、いくら」だけ。** 判断の理由・パターン名・罫線の状態は一切送らない。

副産物として **完全なキルスイッチが手に入る**。契約が切れたら決定の配信を止めるだけでよい。
ローカルにロジックを置いた場合、一度渡したら回収不能(`_billing_lockdown.py` のような
ライセンスチェックはローカルなので外される)。**これは server-side でしか得られない性質。**

### 3-3. それでも漏れるもの(隠せないので、対処を決めておく)

| 漏れるもの | なぜ | 対処 |
|---|---|---|
| **BET額の並び = SEQ の階段の形** | 自分の Stake 口座の履歴に全部残る。数百手で復元される | **隠せないと割り切る**(オーナー決定 2026-09-12: SEQはサーバー側で計算し金額だけ送る)。<br>守れるのは **①リセット規則 ②loss_cut ③開始額の決め方 ④どの形状をいつ使うか**。<br>階段の数列そのものは漏れる前提で商談する |
| 決定ログ + 公開されている罫線 → **統計的な逆算** | 卓の罫線は誰でも見える。(罫線状態, 出した側) の組を1万手貯めればルールを当てにいける | 完全には防げない。緩和 = ①契約で縛る ②パターン名を送らない ③見送り(LOOK)も混ざるのでノイズになる。<br>**「難しいが不可能ではない」と認識した上で商談する** |
| 卓の選定基準 | 接続する卓のリストは見える | 除外卓4つ(MEGA/Big Small/Amazing/バカラ3)程度なら実害小 |

### 3-4. 韓国版GUIで**消す**もの

| 消す対象 | 理由 |
|---|---|
| SEQ パネル・階段プレビュー(GUI内で **65箇所**が "SEQ") | 資金管理の設計が丸見え |
| パターン名・罫線表示(テレコ/ニコニコ/サンサン/ドラゴン/ニコイチ/珠盤路/大路) | 手法そのもの |
| 「追従」表示(**37箇所**)・「逆張り」(**36箇所**) | 派生機能の存在自体が情報 |
| TIME RATE / 勝率レンジ / 拾NOW率チャート | どの条件で強いかのヒント |
| テレグラム関連UI 全部 | 韓国にはchを作らない |
| デバッグログの詳細出力 | `[PATTERN]` 等の行が全部ヒント |

**★削ってから翻訳する。** 逆順にすると要らない物まで訳すことになる(§6参照)。

---

## 4. ★ドメイン問題 — 2026-09-12 に実測して前提が1つ変わった

オーナー入手: **`https://playstake.io`** = Stake 公式のミラードメイン一覧ページ。
ライブサポート曰く「どのURLでもバックシステムは同じ、通常のログイン情報で入れる」。

### 4-1. 実測結果 (2026-09-12 09:22 JST・日本の自宅回線から)

| 対象 | 結果 | 判定 |
|---|---|---|
| **`stake.com`** | **HTTP 451** + 本文に `Legal Reasons` | ★**法的ジオブロック。日本からは死んでいる** |
| ミラー121本(`stake.bet` `stake.games` `stake.jp` `stake.ac` `stake.krd` `stake.pet` `stake1017〜1095.com` `stake3000〜3199.com` …) | **HTTP 403** + 本文に `Just a moment` / `Enable JavaScript` | **Cloudflare の JSチャレンジ。= bot判定であって遮断ではない。実ブラウザなら通る=生きている** |
| **`dga.pragmaticplaylive.net`** | **応答あり**(404 は WS エンドポイントなので正常)。DNS = **AWS 3.165.39.x** | ★**Cloudflare 非経由。シグナル源は無傷** |
| `playstake.io` 自体 | HTTP 200 / `Server: cloudflare` / `forum.stake.com` `cdn.sanity.io` へリンク | Stake 公式で間違いない(フィッシングではない) |

### 4-2. ★「非CDN URL」という理解は不正確

**ミラーも全部 Cloudflare の上にいる**(`Server: cloudflare` / `CF-RAY: ...-NRT` / `__cf_bm` cookie)。
CDN を迂回しているのではなく、**まだ 451 のブロック対象リストに載っていないだけ**。

7/23 の機序(総務省 → Cloudflare 通知 → 当該ドメインを日本向けに 451)は**ドメイン単位**なので、
**ミラーは1本ずつ通知される度に死ぬ**。

そしてそれは既に起きている証拠がある:

```
stake1017, 1020, 1021, 1022, [1023-1033 が欠番], 1034, 1035 ...
stake3020, [3021-3025 が欠番], 3026 ...
stake3000.com → 301 → stake3015.com   (Stake 自身が古いミラーを転送している)
```

★**連番の歯抜けは、焼かれたドメインの墓場。** Stake にとってドメインのローテーションは
例外ではなく**通常運転**。121本もあるのはそのため。

### 4-3. ★これが設計に与える影響(重要)

当初計画では「`BACOPY_STAKE_ORIGIN` を1本の env にして置換」で足りるつもりだった。
**それでは足りない。** ドメインが焼かれる度に**インストーラを11本作り直して配り直す**ことになる。

**要件を格上げする:**

| 要件 | 内容 |
|---|---|
| **オリジンはサーバーが配る** | 受け子は起動時/定期的にマスターAPIへ「今どのドメインを使うか」を問い合わせる。exe に焼かない |
| **フェイルオーバー** | 受け子が **451 を検知したら**、サーバーから貰ったミラーリストの次の候補へ自動で切り替える |
| **サーバー側の生存監視** | VPS から候補ドメインを定期的に叩き、451 になったものをリストから落とす(旧 `_vps_block_watch.py` が近い。root cron `17 * * * *` にあった) |
| **一覧の供給元** | `playstake.io` を定期取得してリストを更新する。Stake 自身が更新してくれる |

→ これで**ドメインが焼かれてもサーバーの値を変えるだけ**になり、再配布が不要になる。
①②③ 全チームで共通の仕組みなので、**Phase 1 の共通基盤に入れる**。

### 4-4. コード側 — 置換箇所は特定済み

| ファイル:行 | 内容 |
|---|---|
| `bacopy_executor_pragmatic_ws_live.py:123` | `LOBBY_URL = "https://stake.com/ja/casino/games/..."` |
| 同 `:1683` | `if ("stake.com" not in low) and ("stake" not in low)` … ホスト判定 |
| 同 `:1955` `:1979` `:4676` `:4701` | `"stake.com/_api/websockets" in url` … ★**WSフレームの選別条件。ここが一致しないとフレームを1つも拾わず、無言で止まる** |
| 同 `:4882` | ロビーURLのハードコード直書き(復帰処理) |
| 同 `:1878` | `casinoId = "ppcds00000003709"` (env で上書き可) |
| `copytrade_gui/src/main.js:608` | GUI 側のロビーURL既定値 |
| `.env.dist` / `.env.template` / `build_staging/.env` | 配布テンプレート |

★`stake.com/_api/websockets` の4箇所が**最も危険**。ドメインだけ変えてここを直し忘れると、
エラーも出さずに「BETが飛ばない」状態になる。ホスト判定は**ヘルパ関数1本に集約**すること。

### 4-5. Phase 0 で残っている未知(実ブラウザでしか分からない)

上の実測で **①ミラーは生きている ②stake.com は死んでいる ③dgaフィードは無傷** までは確定した。
残りは実ログインが要る:

| 確認項目 | 外れた時の影響 |
|---|---|
| **`casinoId` は同じか** (`ppcds00000003709`) | ★最悪ケース。別スキン扱いなら卓ID(qpid)の対応が全部ずれ設計やり直し。<br>ただしサポートが「同じバックシステム・同じログイン」と言っており、Pragmatic から見て同一オペレーターなら**同一である公算が高い** |
| `lpbet` の WS ホストは同じか (`cbcf6qas8fscb222`) | BET送信経路の作り直し |
| Pragmatic の iframe / ゲーム起動フローは同じか | ロビーDOMのクリック経路の作り直し |
| ログインセッションはドメインごとに別か | cookie はドメイン単位なので**別になるはず**。受け子8〜11人分の再ログイン手順が要る。<br>★フェイルオーバーで別ドメインへ移った瞬間に**未ログイン状態になる**点に注意 |
| CDP接続の Chrome が JSチャレンジを通れるか | 受け子は `chrome_attach`(手動起動した実Chromeへ:9222でアタッチ)なので**通る公算が高い**。<br>8/03 に VPS の headless camoufox だけが弾かれ、**受け子の実Chromeは無傷**だった実績と整合 |

---

## 5. 開発フェーズ

### Phase 0 — 実機スパイク (最優先・半日〜1日・オーナー1人/ブラウザ1つ)

§4-1 で**ネットワーク層は実測済み**なので、残りは**実ログインが要るものだけ**。

1. ミラー1本(例 `stake.bet`)に**実Chromeで手動ログイン** → JSチャレンジを通過してロビーが出るか
2. DevTools の Network → WS で **`casinoId` を実測** → `ppcds00000003709` と一致するか ★最大の分岐
3. WS のホスト名を記録 → `<mirror>/_api/websockets` の形か(§4-4 の4箇所の書き換え先)
4. 卓に入り `lpbet` のフレームとホストを記録
5. **$0.20 を手で BET** → 受理されるか
6. 別ドメイン(例 `stake.games`)でも 1〜2 を繰り返す → **ミラー間で casinoId が同じか**
   ★これが同じなら「どのドメインでも同じ設定で動く」= フェイルオーバー設計が成立する
7. 韓国側でも1回(②の前提確認)。韓国は 451 が無いのでどのドメインでもよい

**判定**: `casinoId` がミラー間で同一 + $0.20 が通れば計画通り。違えば §4-5 に戻る。

### Phase 1 — 共通基盤 (Phase 0 通過後・2〜3日)

1. ~~**3系統の `dual_line_*.py` を単一化する**~~ → **✅ 完了。ただし方針を変更した**
   当初「直下版を `_archive_regression/` へ隔離」と書いたが、**実行すると壊れる**ことが判明:
   **`bacopy_api.py`(VPSに載せる本体・57行目 `from dual_line_match import`)を含む約25本が
   直下版を import している**。移動・削除は禁止。
   代わりに実施したこと:
   - **`DUAL_LINE_VARIANTS.md` を作成** — 3系統をサイズ+SHA256付きで確定し、用途別の「正」を表にした
   - **★`_deploy_vps.ps1` に実行停止ガードを追加** — これが真の地雷だった。
     直下(回帰版)を `/opt/laplace2` へ SCP で上書きする作りで、しかも解約済みIPと
     存在しない `E:\` パスを指していた。実行すると即 `exit 1` する
   - ★**朗報**: 中核の `dual_line_logic.py` は直下と `_vps_prod` で**ハッシュ完全一致**
     (`8dde1db81f8d`)。分岐しているのは外殻(bot/money/match)だけで、6/10パターンの判定は
     どの系統でも同じ物。**②のサーバー側 `decide()` にどれを使うか迷う必要がない**

2. **ドメイン抽象化 + サーバー配信 + フェイルオーバー**(§4-3・§4-4)
   - ホスト判定をヘルパ関数1本に集約(★`_api/websockets` の4箇所を漏らさない)
   - マスターAPI に `GET /api/origin` を追加 → 受け子は起動時と定期的に現在のオリジンを取得
   - 受け子側: **451 を検知したら次の候補へ自動切替**(切替後は未ログインになる点をUIで通知)
   - VPS 側: 候補ドメインの生存監視 cron(旧 `_vps_block_watch.py` が近い)+ `playstake.io` の定期取得
3. ~~`collector_pragmatic.py` の `camoufox` 遅延import化~~ → **✅ 完了。ただし遅延importだけでは足りなかった**
   `_vps_prod` 版は dga直結フィードを**ブラウザと併走**させる作りで、Camoufox は依然起動していた。
   **`BACOPY_DGA_ONLY=1` を追加**し、ブラウザを一切起動しないモードを新設(監視ループは共通化)。
   - **実測で検証済み**: 偽 `camoufox` モジュールで import を遮断した状態で 70秒走行 →
     `msgs=625 / shuffles=3 / tables=60 / exit=0`。**必要な pip は `websockets` と `requests` だけ**
   - 失うのはロビーDOM由来の qpid スキャンのみ(卓IDは dga フィード側から取れている)
4. ~~**旧IP `210.131.215.116` の完全除去**~~ → **✅ 完了**(実行系から全消去・残るは説明コメントのみ)。処理した箇所:
   - ★`copytrade_gui/build_staging/.env:2`(**全インストーラへ焼き込まれる雛形**。
     しかも同ファイルの `BACOPY_LOBBY_URL` が**ロビー直リンクのまま**=冷間起動で必発のバグ)
   - `scripts/provision-user-build.js:206,273` / `copytrade_gui/main.js:909` / `_verify/` `_asar_tmp/` `_asar_peek/` / `.env.dist:15` / `.env.template:27`
   - **既存の `dist/BACOPYRECEIVER_user01..11_Setup.exe` は配布禁止**(旧IP内蔵)
5. VPS 契約(1vCPU/1GB で足りる)+ Caddy + systemd + DNS(`master.bafather.uk`)
6. **鍵を全て新規発行**。`CREDENTIAL_REVOCATION_CHECKLIST_2026-08-22.md` の🔴6件が**全て未着手**なので、
   ここで一緒に片付ける(ANTHROPIC_API_KEY revoke / Stakeパスワード変更 / STAKE_API_TOKEN失効 /
   Telegram bot トークン3本再発行)

### Phase 2 — ③ 田辺チーム(最小・実証済み)(3〜4日)

**なぜ最初か**: 8/28に一周実証済みで差分が最小。かつ**ドメイン切替とフェイルオーバーを本番で実証する
パイロット**になる。①(本命)を出す前にここで URL を潰しておく。

1. `bacopy_api.py` + `collector_pragmatic.py` の2本をVPSへ(ブラウザ不要)
2. 認証ゲート除去(`billingStatus()` のバイパス。呼び出し元は `src/main.js:2163` の1箇所のみ)
3. 受け子ビルド(mirror プロファイル)→ **1台で $0.20 テスト** → ack/result がマスターへ返るか
4. 通ったら残りへ展開
5. ★仕様の注意: マスターが送るのは**卓と側だけ**。金額は受け子側の設定で決まる
   (`bacopy_executor_pragmatic_ws_live.py:6039`)。「いくらで回すか」は事前に決めて配る

### Phase 3 — ① 梶原チーム(復元)(4〜5日)

**方針: 構造を変えない。** 復元中にアーキテクチャを変えると障害切り分けが不能になる。

1. `_vps_prod/laplace2/` からシグナルbotを復元(dga直結モード)
2. **テレグラム4ch を新規作成**(6パターン / 10パターン / 追従監視 / 時間ごと勝率)
   - bot トークンは新規発行(旧トークンは失効対象)
   - 宛先は `.env`(`DUAL_LINE_CHAT_ID` `DUAL_LINE_RESULT_CHAT_ID` `WINRATE_CHAT_ID` `DUAL_LINE_EXTRA_CHAT_IDS`)
   - `hourly_report.py` は root cron。8/03 以降**カンマ区切りで複数宛先対応済み**
   - ★**7/30 に番号変更で3ch消した前科あり**。作り直したら ID を `reference_telegram_channels.md` に即記録
3. 受け子ビルド(dual_line プロファイル・非CDN URL・SEQ 有効)
4. ★`loss_cut=0` で配らない(user04 全損の直接原因)
5. ★**`.env` を変えたら、それを source した親プロセスを再起動する**。
   子だけの再起動では旧環境が継承される(8/22 に実際に配信が止まらなかった機序)

### Phase 4 — ② 韓国チーム(新規・最も重い)(1.5〜2週間)

1. **`brain.py` を新規作成**(サーバー側決定エンジン)
   - dga直結フィードから結果履歴を組み立て → `decide(history, next_n)` を呼ぶ
   - `bacopy_api.py` の decision キューへ投入。**③の配管をそのまま再利用**
   - ★出力payloadは `{table, side, amount}` のみ。`china_pattern` / `big_pattern` を**含めない**

2. **★SEQ をサーバー側へ移す**(オーナー決定 2026-09-12)
   ③(mirror)には無かった要件で、**②で唯一の非自明な追加開発**。

   | 論点 | 内容 |
   |---|---|
   | 状態の置き場 | `dual_line_money.py` は現在 `dual_line_money_state.json` にローカル保存(`src/main.js:1313`)。<br>**executor別のレコードとして sqlite へ移す**必要がある |
   | ★決済フィードバックのループ | **SEQ の段は「結果が確定してから」進む。** つまり brain は<br>`決定を送る → 受け子が BET → 勝敗が返る → 段を進める` の**往復**を持つ。<br>③は金額がローカルなのでこのループが要らなかった。**ここが新規配管** |
   | 取りこぼし時の扱い | 受け子が BET に失敗した/ack が返らない時に段を進めてはいけない。<br>**幻SEQ**(user07で発生した、実BET無しに階段だけ進む事故)を再現しない設計にする |
   | クラッシュ復帰 | サーバー再起動で段が飛ばない/巻き戻らないこと。DBが正 |
   | `loss_cut` | **ゼロで配らない**。サーバー側で強制する(クライアントに設定させない) |
3. **韓国向け engine を別ビルドにする**
   - `dual_line_pragmatic_bot.py` / `dual_line_logic.py` / `dual_line_match.py` / `dual_line_money.py` を
     **spec から除外**(難読化ではなく**不在**にする)
   - ★現状 exe は1本で2モードを持つ構造なので、**KR用に entry を分けた spec が要る**。ここは要調査
   - ★**検証は文字列検索でやらない**。実行して「dual-line サブコマンドが存在しないこと」を確かめる
     (`SEQ_CUSTOM_START_2026-08-05.md` の教訓)
4. **GUI の機能削減**(§3-4)→ その後 i18n(§6)
5. **インフラを分離する**
   - KR は**別VPS・別APIキー・別executor名前空間**。JP の決定を見せない/触らせない
   - 韓国/日本リージョンに置いてレイテンシを詰める
   - 万一漏れた時に**そのサーバーだけ落とせる**ようにしておく
6. 実機テスト(韓国側デスクトップクラウド1台で $0.20)

### Phase 5 — 展開・監視

1台ずつ ack を確認してから次へ。3チーム分の監視を1つのマスター画面に集約。

---

## 6. 韓国語化(i18n)の進め方

**現状: i18nの仕組みは存在しない。** 日本語がソースに直書きされている(本日実測)。

| ファイル | ユニークな日本語語句 |
|---|---|
| `src/renderer/index.html` | 324 |
| `src/renderer/app.js` | 626 |
| `src/main.js` | 395 |
| **合計** | **1,269** |

そのままだと1,269語の翻訳になるが、**§3-4 で機能を削ってからやれば大幅に減る**
(SEQ/追従/逆張り/チャート/テレグラム関連のUIが丸ごと消えるため)。

手順:
1. 先に**削る**(§3-4)
2. 残った表示文字列を `locales/ja.json` / `locales/ko.json` へ抽出し、`t('key')` に置換
3. ロケールは `.env` の `BACOPY_LOCALE=ko` で切替
4. ★`main.js` の395語の多くは**ログ文字列**でUI非表示。翻訳対象から外す。
   ただし**ログにも用語が出るので、韓国版ではログ自体を削るか英語の内部IDに置換する**

---

## 7. ビルド構成 — 1コードベース / 3テナントプロファイル

`copytrade_gui/scripts/provision-user-build.js` は既にユーザー別ビルドを持っている。
これを**テナント別**へ拡張する。

```
profiles/
  jp-kajiwara.json  mode=dual_line  origin=<非CDN>   locale=ja  telegram=on   brain=local   money=SEQ
  jp-tanabe.json    mode=mirror     origin=<非CDN>   locale=ja  telegram=off  brain=master  money=flat
  kr-partner.json   mode=thin       origin=stake.com locale=ko  telegram=off  brain=server  money=SEQ(server)
                                                                              ↑ロジック非同梱・金額も受信するだけ
```

★`kr-partner` だけは **engine の spec が別**(ロジックファイルを含めない)。
ここを「同じexeで env で切替」にしてしまうと、**バイナリの中にロジックが入ったまま配ることになる**。
プロファイルの差ではなく**成果物の差**にすること。

---

## 8. 見積り

| Phase | 内容 | 目安 | 依存 |
|---|---|---|---|
| 0 | ミラー実機スパイク(ネットワーク層は実測済) | **半日〜1日** | ★全ての前提 |
| 1 | 共通基盤・3系統の単一化・**オリジン配信+フェイルオーバー**・旧IP掃除・VPS・鍵 | **3〜4日** | Phase 0 |
| 2 | ③ 田辺(mirror) | 3〜4日 | Phase 1 |
| 3 | ① 梶原(復元 + テレグラム4ch) | 4〜5日 | Phase 1 |
| 4 | ② 韓国(brain.py + **SEQサーバー化** + 削減 + 韓国語 + 別ビルド) | **2〜2.5週** | Phase 2の配管 |
| 5 | 展開・監視 | 随時 | — |

**Phase 2 と 3 は並行可能**(別チーム・別プロファイル)。Phase 4 は Phase 2 の配管が要る。

---

## 9. 意思決定 — 2026-09-12 にオーナー確定

| # | 論点 | **決定** | 備考 |
|---|---|---|---|
| 1 | ②のロジック配置 | **サーバー側で計算** | `decide()` はオーナーのサーバーにのみ存在。受け子へは `{卓,側,金額}` のみ。キルスイッチが手に入る |
| 2 | ②の資金管理 | **SEQ をサーバー側で計算し、金額だけ送る** | ★階段の**数列は口座履歴から復元される**ので隠せない。守れるのはリセット規則・`loss_cut`・開始額の決め方。<br>★実装上は**決済フィードバックのループが新規に必要**(Phase 4-2) |
| 3 | 着手順 | **Phase 0 → ③田辺 → ①梶原 → ②韓国** | ③で非CDN URLを本番実証してから①へ |
| 4 | ①もサーバー側に寄せるか | **今回はやらない** | 復元中に構造を変えない。落ち着いてから、ローカルdga(旧VPS比8秒速)の差が**現行のdga直結VPSでも残るか**を実測してから判断 |
| 5 | ②のインフラ | **別VPS・別APIキー・別executor名前空間** | 共用しない。漏洩時にそのサーバーだけ落とす |
| 6 | 契約 | **技術対策と別に必要** | 再配布禁止・リバースエンジニアリング禁止・期間。<br>§3-3 の「統計的逆算は完全には防げない」を前提に条項を作る |

### 未確定(進行に伴い決める)

- 非CDN URL の具体的な一覧(Phase 0 で使う)
- ①②③ それぞれの受け子の人数 → インストーラの本数
- ②の開始額・SEQ形状(サーバー側設定なので後から変えられる)

---

## 10. 既知の罠(3チームとも最初から入れる)

| 罠 | 対処 |
|---|---|
| Chrome 冷間起動で「Failed to start third party session」 | ロビー直リンク禁止。`casino/home` 経由(非CDN URLでも同じ形にする) |
| 個人Chromeと `:9222` の競合でGUIクラッシュ | 賭け用は専用プロファイル |
| 受け子ごとに BET コードが違う (Banker=10/Player=11) | `.env` の `bc` |
| 賭けChrome の膨張(2GB+ でクリック遅延) | GUIバッジで監視。解消はOS再起動か `:9222` kill |
| 古いChromeでサポートモーダル連発 | Chrome 149 以降 |
| `loss_cut=0` | ゼロで配らない |
| `.env` を変えても止まらない/効かない | **source した親プロセスを再起動**。`/proc/<pid>/environ` で確認 |
| 停止し残し | systemd / cron / **手動nohupの孤児** の3経路で探す |
| `.gitignore` の `_*.py` | `find | wc -l` と `git ls-files | wc -l` を突合 |
| exe の中身の検証 | **文字列検索でなく実行**して確かめる |
| ssh越しの `pkill -f` | **自セッションを殺す**。PIDを目視して kill |
| **ミラードメインが 451 で焼かれる** | オリジンを exe に焼かない。サーバー配信 + 自動フェイルオーバー(§4-3)。★連番の歯抜けが墓場 |
| ドメイン切替直後は**未ログイン** | cookie はドメイン単位。切替をUIで通知し、再ログイン導線を出す |
| `stake.com/_api/websockets` の判定を直し忘れる | **エラーを出さずBETが飛ばなくなる**。ホスト判定はヘルパ1本に集約 |

---

## 11. 参照

| 文書 | 内容 |
|---|---|
| `RESTART_KIT_2026-08-22.md` | 停止の全体像・金庫の中身・**3系統の dual_line_*.py の罠** |
| `MIRROR_REVIVAL_PLAN_2026-08-28.md` | ③の下調べ・ローカル一周実証・Supabase不要の根拠 |
| `INCIDENT_VPS_CLOUDFLARE_DGA_DIRECT_20260803.md` | dga直結化・障害切り分けの型 |
| `RISK_JP_CDN_BLOCKING_2026-08-06.md` | 日本向けCDNブロッキング(★9/12 に `stake.com` 自体が 451 化。同文書の「stake.comは未対象」は失効) |
| `CREDENTIAL_REVOCATION_CHECKLIST_2026-08-22.md` | 旧鍵の失効(🔴6件が未着手) |
| `USER_COMPARISON_KATSUZAWA_VS_KAJIWARA_2026-08-01.md` | 損益を決めるのは設定でなく運用/entry signal |
| `SEQ_CUSTOM_START_2026-08-05.md` | exe検証の教訓 |
| `docs/friend_proxy_betting_system.md` | ミラー版の設計思想(③の原点) |

---

*作成: 2026-09-12*
