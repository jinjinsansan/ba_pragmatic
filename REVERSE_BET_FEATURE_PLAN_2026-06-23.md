# 【次回作業】逆張り(reverse)モード — 設計メモ  2026-06-23

## 要望（梶原さん/ユーザー）
v3(6パターン)/v4(10パターン)のNOWが出たら、**本来の逆をBET**する機能。エッジが無いのは承知済み＝基本使わないが、「手動で逆に賭けたい時に、オートで一時的に逆にする」用途。

## 設計の核（低リスク）
**decision の side(B/P) を“入口で反転”（B↔P・Tはそのまま）だけ。** これで賭け・決済・光り・勝率カウントが全部一貫（エンジンは反転後sideで賭け、反転後sideで決済）。**タイミング/センタリング/決済の経路は無変更**＝2026-06-23に壊れた敏感な所に触らない。値の反転のみ＝安全。

### 実装ポイント
- エンジン: `self._reverse_bet` フラグ。VPS decision の side を読む入口で `if self._reverse_bet: side = {'B':'P','P':'B'}.get(side, side)`。stdin コマンド `{"type":"set_reverse","on":bool}` で即時切替（再起動不要）。**money modeコントロールの仕組みを流用**。
- GUI: トグル「逆張り」＋ stdin送信＋状態表示。v3/v4両方のNOWに適用。

## ★逆張り × 追従(follow) の相互作用【ユーザー確定の設計判断】
**逆張りは追従してはいけない。** 追従は「勝った後にパターンの順方向で連鎖」する戦略で、その論理は**順方向(本来のside)前提**。反転したBETに追従すると論理が破綻（無エッジを変な形で複利化）。
→ **逆張りON時は、GUIで追従が選択されていても、追従を自動停止する。**

### 実装
- エンジンの追従発火 (`_follow_on_settled` 等) を `self._reverse_bet` でゲート：
  `follow_active = self._follow_enabled and not self._reverse_bet`
- ＝**GUIの追従選択(`_follow_enabled`)は保持したまま、逆張りON中だけ追従を抑止**。逆張りOFFに戻すと追従も復帰（GUIで選択中なら）。
- GUI表示：「逆張りON（追従は自動停止中）」と明示。

## ★安全設計（一番大事＝「付けっぱなし事故」防止）
切り忘れると無自覚に逆BETし続けて損するので：
1. **ON中は赤い大きな常時表示**（GUI上部「⚠逆張りON」）
2. **再起動でOFFに戻す（永続させない）**＝事故で残らない・毎セッション明示ON
3. 任意：**ON継続でTelegram警告**（「逆張りNN分ON」）

## 勝率の扱い
反転した実際の賭けを記録（一貫）。**素の6P/10P勝率を見たい時は逆張りOFF**が前提。

## 展開手順（順守）
1. **bafatherで実装→トグルON/OFFでsideが反転するか・勝敗が正しく付くか・逆張りON中は追従が止まるか実測**
2. 確認後に受け子リビルド→配布。
3. 安全網（タグ等）は今日の運用と同じく必ず張ってから。

## 関連
逆張り自体が無エッジなのは [[既存の追従=無エッジ分析]] と同根（反転しても期待値は対称的にゼロ近傍）。この機能は「規律/手動代替」目的であってエッジ取得ではない。
