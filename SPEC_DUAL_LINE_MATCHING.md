# デュアルライン・マッチング予想ロジック & バックテスト 実装仕様書

## 0. この仕様書の目的

中国罫線（珠盤路）と大路の2つの罫線を読み、両者の予想が一致したときのみBETする予想ロジックと、その**バックテスト方法**を厳密に定義する。

本仕様書は **予想ロジックの定義** と **バックテストの実行方法** を扱う。
ベットサイズ・SEQ管理は別仕様とする。

> **実装者（Claude Code）への最重要注意**
> この仕様書の `第II部 バックテスト仕様` には、過去に何度も実装ミスが起きた「逐次リプレイ」の絶対ルールがある。実装前に必ず第II部を読むこと。特に **「未来参照の禁止」** と **「手計算による正解テスト」** は必須要件である。

---

# 第I部: 予想ロジック

## 1. 基本コンセプト

- 左側の **中国罫線（珠盤路）** と 右側の **大路** を独立して分析する
- 1つのシューの中で、**1ハンドずつ前進しながら、毎ハンド両罫線を見比べる**
- それぞれが「次の手」を予想する
- **2つの予想が一致したとき** のみ、次手にBET（方向は一致した予想と同じ）
- **不一致 / 判定不能 / 複数パターン同時該当** はすべて見送り（ルック）
- 「迷ったら打たない」を徹底する

### 1.1 動作スタイル（重要）

これは「テーブルを探して入退室する」ロジックではない。

- 1つのシューに腰を据え、シューが終わるまで毎ハンド判定し続ける
- ほとんどの手は見送りで、条件が揃った瞬間だけ打つ「待ち」のスタイル
- 1シューあたりのBETチャンスは数回〜十数回程度を想定

### 1.2 用語定義

| 用語 | 意味 |
|---|---|
| **珠盤路 (Bead Plate)** | 中国罫線。6行×複数列のグリッドに結果が縦方向に埋まる |
| **大路 (Big Road)** | 同一結果の連続を縦に積み、異なる結果が出たら次の列に進むグリッド。T は記録しない |
| **P** | Player 勝ち |
| **B** | Banker 勝ち |
| **T** | Tie（タイ） |
| **見送り（ルック）** | BETせず結果を見る |
| **行（中国罫線）** | 珠盤路の同一行に並ぶ各列の値。**横方向**に読む。隣同士は「6手ごとに飛んだ手」になる |

---

## 2. 中国罫線（珠盤路）の構築と読み方

### 2.1 珠盤路の埋まり方

- 6行のグリッド
- 結果が出るたびに、**一番左の列の上から下** へ順番に埋める
- 6行目（最下段）まで埋まったら、**1つ右の列の1行目** に進む
- T（タイ）もそのままマスに記録する（スキップしない）

### 2.2 見る行の決定

珠盤路で**次にBETする手が入るマスの行**を読む。

```
見る行の行番号 = ((N - 1) mod 6) + 1
```

- `N`: 次にBETする手の通算番号（1始まり）
- 行番号は 1〜6 の範囲

| 次の手の番号 N | 見る行 |
|---|---|
| 1 | 1行目 |
| 6 | 6行目 |
| 7 | 1行目 |
| 38 | 2行目 |

### 2.3 行の読み方（★過去のバグの原因。厳密に）

決定した行を **横方向** に、一番左の列から右へ読む。

**重要**: 同じ行に並ぶマスは、**通算手数が6ずつ飛んだ値**になる。連続した手ではない。

**具体例**（通算1〜13手が以下の順で出た場合）:

```
1:P  2:B  3:P  4:B  5:P  6:B
7:P  8:P  9:B 10:B 11:P 12:B
13:P
```

珠盤路グリッド:

```
        1列目   2列目   3列目
1行目:   P(1)    P(7)    P(13)
2行目:   B(2)    P(8)
3行目:   P(3)    B(9)
4行目:   B(4)    B(10)
5行目:   P(5)    P(11)
6行目:   B(6)    B(12)
```

- 2行目を横読み → `[B(2手目), P(8手目)]` = `B P`
- 1行目を横読み → `[P(1手目), P(7手目), P(13手目)]` = `P P P`

→ 隣同士が「6手ごとに飛んだ手」であることを必ず守ること。連続手を読んではいけない。

### 2.4 判定の前提条件（必須）

以下のいずれかを満たさない場合、中国罫線側の予想は **「判定不能」**（→ 見送り）。

| 条件 | 内容 |
|---|---|
| **最小マス数** | その行に埋まっているマスが **3マス以上** あること |
| **T 不含** | その行に T が **1つも含まれていない**こと（含む行は一律見送り） |

### 2.5 認識するパターン（4種類）

行（横並び、P/Bのみ）を左から右へ評価する。

#### パターンA: テレコ
- **条件**: 行が **P と B の完全な1個ずつ交互**（PBPB... / BPBP...）
- **予想**: 交互の続き（直近マスの逆）
- 例: `P B P B` → 次は `P`

#### パターンB: ニコニコ
- **条件**: 行が **2個ずつ交互**（PPBB / BBPP の繰り返し）
- **予想**: パターン継続
- 例: `P P B B` → 次は `P`

#### パターンC: Pライン / Bライン 【2026-06-07 改定】
- **条件**: 行から **T を除いた P/B が 5 個以上** かつ **逆側が累積 1 個まで**（5個なら 4対1 or 5対0）
  - T は無視して P/B だけで数える
  - 逆側が 2 個目に達したら線は終了（見送り）
- **予想**: 多数側を順張り継続。Pライン → 次は `P` / Bライン → 次は `B`
- **継続**: 線が続く限り（逆側 ≤1 のまま）次のマスもライン予想とする
  （例: `PPPPP` の後に6個目が B でも、それが唯一の逆なら線継続で7個目も P 予想）
- ⚠️ **旧定義（〜2026-06-06）**: 「P/B が 3 個以上」の多数決（連続不問）。`PPBBP` のような
  散らばりも誤って Pライン扱いしていた。3個以上では大サンプルで全組み合わせマイナス。
  5個以上・逆1個・T無視に改定し、Bライン｜テレコ(B) 等で薄エッジが出るようになった。

#### パターンD: サンサン（3個3個狙い）★新規
- **条件**: 行が **ちょうど `PPPBB` または `BBBPP`**（3連 + 2連、計5マス）
  - 最初の塊は **ちょうど3連**（PPP / BBB）。4連以上（PPPP等）は対象外
  - 次の塊は2連
- **予想**: 3連3連を完成させる方向
  - `PPPBB` → 次は `B`（PPPBBB を完成させる）
  - `BBBPP` → 次は `P`（BBBPPP を完成させる）

### 2.6 複数パターン同時該当時の扱い（★過去のバグ箇所）

上記A・B・C・Dのうち **2つ以上に同時該当する場合**、中国罫線側の予想は **「判定不能」**（→ 見送り）とする。ただし、サンサンとPライン/Bラインのように構造上必ず重なるものは「3. パターン優先順位」で順序を定義して解決する。

> **実装上の警告（telecho/niconico と p_line/b_line の競合バグ）**
> 過去の実装で、Pライン/Bラインの判定がテレコ・ニコニコと競合し、BET数が激変するバグが発生した（telecho|telecho|B が 2,571→1,137 に減少）。
> 原因は「テレコやニコニコに該当する行が、同時にPライン条件（P3個以上）も満たしてしまう」ケースの処理が曖昧だったこと。
> 解決は次章「3. パターン優先順位」の判定順序を厳守すること。「最初に見つかったパターンで確定」のような早期確定をしてはいけない。

---

## 3. パターン優先順位（★サンサンを活かすための必須ルール）

「複数該当は見送り」をそのまま適用すると、サンサン（PPPBB）はPライン（P3個）と必ず衝突して機能しなくなる。これを避けるため、判定の順序と優先順位を以下に定める。

### 3.1 判定順序

中国罫線の行に対し、以下の順で判定する:

1. **まず「テレコ」「ニコニコ」「サンサン」の3つ（=形状パターン）を判定する**
2. 形状パターンのうち該当が **ちょうど1つ** なら、その予想を採用して**確定**（Pライン/Bラインは見ない）
3. 形状パターンの該当が **2つ以上** なら → 見送り（判定不能）
4. 形状パターンの該当が **0個** のときのみ、**Pライン/Bラインを判定する**【2026-06-07 改定】
   - 行から T を除いた P/B が **5個以上** かつ **逆側 ≤1個** → 多数側を順張り
     （Pラインのみ → `P` / Bラインのみ → `B`）
   - P/B が 5個未満、または逆側が 2個以上 → 見送り
   - （5個以上で逆側 ≤1 なら必ず一方が多数＝両側同時該当は起こらない）

### 3.2 この順序にする理由

- サンサン `PPPBB` は形状として明確なので、Pライン（単なる多数決）より優先して拾う
- 形状パターンが見えないときの保険としてPライン/Bラインを使う
- これにより `PPPBB` を「P多数だからPライン→次P」と誤読せず、「サンサン→次B」と正しく読める

### 3.3 優先順位の具体例

| 行 | 形状判定 | 最終予想 |
|---|---|---|
| `P P P B B` | サンサンのみ該当 | サンサン → 次B |
| `P P B B` | ニコニコのみ該当 | ニコニコ → 次P |
| `P B P B` | テレコのみ該当 | テレコ → 次B |
| `P P B B P` | 形状0個・計5個だが逆B 2個 → 線にならない | 見送り【新定義】 |
| `P B P B P` | テレコのみ該当（形状1個）| テレコ → 次P |
| `P P P P B` | 形状0個・P4 B1（逆1）計5個 → Pライン | Pライン → 次P |
| `P P P P` | 形状0個・計4個（5未満） → 線にならない | 見送り【新定義】 |
| `B B B B T B` | 形状0個・T除き5B（逆0） → Bライン | Bライン → 次B |

---

## 4. 大路の構築と読み方

### 4.1 大路の構築

- T を無視した P/B の時系列をもとに列構造を作る。各列は同一結果の連続
- 例: `B P B B P P T P` → 列: `[B][P][BB][PP][P]`、末尾が現在進行中の列

### 4.2 認識するパターン（4種類、すべて順張り）

#### ドラゴン
- **条件**: 最新列が **4連以上** の同一結果
- **予想**: その側を継続（PPPPP → 次P）

#### テレコ
- **条件**: 末尾 **4手以上が PBPB... の完全交互**（各列長1で交互）
- **予想**: 次は逆（直近手の逆）

#### ニコニコ
- **条件**: 末尾が **PPBBPPBB の8手以上**揃った時点で認識（2連が交互）
- **予想**: パターン継続

#### ニコイチ
- **条件**: 末尾が **PPBPPB の6手以上**揃った時点で認識（2-1の繰り返し）
- **予想**: パターン継続

### 4.3 複数該当・非該当

複数該当・どれにも非該当はいずれも **判定不能**（→ 見送り）。

---

## 5. マッチング判定（最終BET決定）

### 5.1 判定フロー

1. 中国罫線の予想を取得（P / B / 判定不能）
2. 大路の予想を取得（P / B / 判定不能）
3. 下表で最終判定:

| 中国罫線 | 大路 | 最終判定 |
|---|---|---|
| P | P | **BET: Player** |
| B | B | **BET: Banker** |
| P | B | 見送り |
| B | P | 見送り |
| 判定不能 | 任意 | 見送り |
| 任意 | 判定不能 | 見送り |

### 5.2 鉄則

**不一致・判定不能・複数パターン該当 — いずれも見送り。「迷ったらBETしない」。**

### 5.3 パターンの組み合わせ記録（集計用）

BETが発生したら、どのパターン同士のマッチングだったかを記録する。形式:

```
中国罫線パターン | 大路パターン | 方向
例: sansan|niconico|B
    pline|dragon|P
    tereko|tereko|B
```

集計時はこの組み合わせごとにBET数・勝敗・勝率・$/BETを出す。

---

# 第II部: バックテスト仕様

> **この第II部が、過去に何度も実装が失敗した箇所である。必ず精読すること。**

## 6. バックテストの目的と前提

### 6.1 目的

63,000シュー（数百万ハンド）の確定済みデータに対し、第I部のロジックを適用し、**もし当時このロジックでBETしていたら、どういう成績だったか**を再現する。

### 6.2 データ前提

- 各シューは P/B/T の結果が **最初から最後まで確定済み**
- だが、バックテストでは **「その時点までしか見えていなかったフリ」をして** 判定する

---

## 7. 逐次リプレイの絶対ルール（★最重要・過去の失敗原因）

### 7.1 やってはいけないこと（禁止事項）

> **❌ 禁止1: シュー全体を一度に見て判定してはいけない。**
> 確定済みデータだからといって、シュー全体の罫線を一括で組み、全体を俯瞰して「ここがBET」と判定するのは誤り。これは「未来をカンニングしている」状態であり、実運用を再現していない。

> **❌ 禁止2: i手目を判定するとき、i手目以降（未来）の結果を一切参照してはいけない。**
> 罫線の再構築に使ってよいのは **1手目〜(i-1)手目だけ**。i手目の結果は「答え合わせ」にのみ使い、罫線構築や予想には使わない。

> **❌ 禁止3: パターン認識を「完成形が見えてから」遡って数えてはいけない。**
> 例えば「このシューにはPPPBBBが出ている」と全体から探して、その箇所をBET扱いするのは誤り。あくまで各時点で前進的に判定する。

### 7.2 正しいループ構造（擬似説明）

```
for 各シュー in 63000シュー:
    for i in 1 .. シューの長さ:
        # ── このとき見てよいのは history[1 .. i-1] だけ ──
        visible = シューの結果[1 .. i-1]   # i手目以降は絶対に渡さない

        # visible だけを使って罫線を再構築
        珠盤路 = build_bead_plate(visible)
        大路   = build_big_road(visible)

        # 「i手目」の予想を行う（N = i）
        中国罫線予想 = predict_bead(珠盤路, N=i)
        大路予想     = predict_big_road(大路)
        判定         = match(中国罫線予想, 大路予想)

        if 判定 == 見送り:
            continue   # BETしない、次の手へ

        # ── ここで初めて i手目の確定結果を見る（答え合わせ） ──
        実際の結果 = シューの結果[i]

        if 実際の結果 == 'T':
            タイ扱い（push、資金増減なし、勝敗カウントに入れない）
        else:
            勝敗 = (判定の方向 == 実際の結果)
            記録する(勝敗, 判定の方向, パターンの組み合わせ)
```

### 7.3 「答え合わせ」の定義

- 予想方向（Player or Banker）と、i手目の確定結果を照合
- `予想 == 結果` なら勝ち、違えば負け
- i手目が T のときは引き分け（push）扱い（資金増減なし、勝敗カウント外）

---

## 8. 手計算による正解テスト（★実装検証の必須ステップ）

### 8.1 なぜ必要か

63,000シューでいきなり検証すると、「出た数字が正しいのか誰も検証できない」状態に陥る（過去にこれが起きた）。
そこで、**人間が手で答えを出せる超小規模なテストシュー** を先に用意し、コードの出力と照合する。一致するまで本番データに進まない。

### 8.2 テストケースの作り方

1. 10〜20手程度の短いシューを5〜10本、手で用意する
2. 各シューについて、**各手で「BETすべきか / 方向は何か / どのパターンか」を人間が手計算**し、正解として書いておく
3. コードに同じシューを食わせ、出力が手計算と完全一致するか確認

### 8.3 必須テストケース（最低6本）

実装者は以下を網羅するテストシューを用意し、各手の期待出力テーブル（見る行・マス数・パターン・予想・結果・勝敗）を作成して、人間（仁さん/友人）にレビューを依頼すること。

1. ドラゴン × Pライン が一致してBETするケース
2. サンサン狙いが発火するケース（珠盤路の行が PPPBB になり大路がBを示す瞬間）
3. テレコ単独で発火するケース
4. ニコニコ単独で発火するケース
5. 複数該当による見送りケース
6. T含み行による見送りケース、および序盤の「3マス未満で見送り」ケース

### 8.4 合否基準

- 6本のテストシュー全手で、コード出力と手計算正解が **完全一致** → 合格 → 本番63,000シューへ
- 1つでも不一致 → 実装バグ。本番に進まない

---

## 9. 集計と出力

### 9.1 全体集計

| 指標 | 内容 |
|---|---|
| 総ハンド数 | 全シューの総手数 |
| 総BET数 | 実際にBETした回数 |
| 見送り数 | 判定不能・不一致でBETしなかった回数 |
| 勝ち / 負け / 引分(T) | 内訳 |
| 勝率 | 勝ち /（勝ち+負け） |
| $/BET | 1BETあたり期待損益 |

### 9.2 パターン組み合わせ別集計

`中国罫線パターン | 大路パターン | 方向` ごとに、BET数・勝率・$/BET を出す（5.3参照）。

### 9.3 サニティチェック（数字の妥当性確認）

実装が正しければ、以下が成り立つはず:

- **総BET数 = 各パターン組み合わせのBET数の合計**（取りこぼし・二重計上がないか）
- サンサン（sansan）のBET数が **0件ではない**（友人の「PPPBBBは頻繁に出る」という直感と整合）
- 同じデータ・同じロジックで再実行したら **数字が完全に再現する**（実行ごとに倍半分でブレるのは異常）

> **過去の異常**: 「旧BETs 2,571 → 新BETs 1,137」のように修正のたびに数字が倍半分で変動した。これは逐次リプレイか競合処理のどちらかが壊れている兆候。第II部7章・第I部2.6/3章を満たせば再現性が出るはず。

---

## 10. 修正・実装ファイル（想定）

| ファイル | 役割 | 新規/修正 |
|---|---|---|
| `dual_line_logic.py` | 第I部の予想ロジック（珠盤路読み・大路読み・マッチング） | **新規** |
| `dual_line_backtest.py` | 第II部の逐次リプレイ・集計 | **新規** |
| `test_dual_line.py` | 第8章の手計算テストケース | **新規** |

### 10.1 dual_line_logic.py に必要な関数（想定）

- `build_bead_plate(visible_results) -> grid`：visibleのみで珠盤路を構築
- `get_bead_row(grid, N) -> list`：N手目に対応する行を横方向（6手飛び）で取得
- `predict_bead(row) -> 'P'|'B'|None`：第I部3章の優先順位で予想
- `build_big_road(visible_results) -> columns`
- `predict_big_road(columns) -> 'P'|'B'|None`
- `match(bead_pred, road_pred) -> ('player'|'banker'|None, pattern_label)`

---

## 11. 範囲外（別仕様で定義）

- ベットサイズ・SEQ進行・〇✖管理
- 損切 / 利確
- 実運用時のテーブル選定・入退室
- GUI表示

---

## 12. 改訂履歴

| 日付 | 変更内容 |
|---|---|
| 2026-05-18 | 初版。中国罫線3パターン + 大路4パターン + マッチング判定 |
| 2026-05-18 | サンサン（3個3個狙い）追加、パターン優先順位を新設、複数該当バグの明示、第II部バックテスト仕様（逐次リプレイ・未来参照禁止・手計算正解テスト）を追加 |
| 2026-06-07 | Pライン/Bラインを「3個以上の多数決」から「**T除き P/B が5個以上 かつ 逆側≤1個 の順張り**」に改定。形状(テレコ/ニコニコ/サンサン)はT含み行は従来通り判定不能。再BT(102k)で `bline\|telecho\|B`=51.7%/+0.0078/Z+1.68 が出たため v4(10パターン) の最弱 `niconico\|niconico\|B` と入替 |
