# v4センタリング遅延＝envの時間予算キャップで実用域（コード無変更）  2026-06-23

## 問題
v4(10パターン)はNOW連発→各NOWで「目的卓へのセンタリング(卓切替/維持)」が重い48卓Chrome上で走り、単一Playwrightスレッドを塞ぐ→決済pump停止→**光り遅延／卓切替の固まり（NOW→BET→光らない・黄色枠残る）／取りこぼし**。計測(`CENTERING_DIAG_2026-06-23.md`)で `executor.tick` が9〜138秒ブロックと判明。v3は無問題（NOW稀）。

## 試して失敗した案
- **①目的タイル直接参照(getElementById fast-path)**: 実測で**効かず**（fast_path_in_view=0・LOOP-SLOW max138s維持）。真因は「画面外の卓への切替」で、①が狙った「画面内の維持再確認」ではなかった。コードは branch `centering-fix-2026-06-23` に退避（不採用）。

## ★効いた案＝梶原さん発「完全無効でなく、光り遅延しそうな時だけ見た目を諦める」
**センタリングに時間予算（デッドライン）を付ける**：速い卓は今まで通り成功、遅い卓は打ち切って光り優先。**WS賭けはタイルDOM不要（`<lpbet>`をWS直送）**＝センタリング諦めても賭けは飛ぶ・光る、を実コードで確認済み。

### 設定（bafather .env・コード無変更・即リバーシブル）
```
BACOPY_MULTI_FOCUS_MS=1500              # センタリング上限 9〜12秒 → 1.5秒
BACOPY_MULTI_PREPOSITION_FOCUS_MS=1500  # ★NOW賭けはpreposition経路なので必須
BACOPY_MULTI_SCROLL_MAX=8               # スクロール探索 48 → 8ステップ
BACOPY_MULTI_PREPOSITION_SCROLL_MAX=8
```
（デフォルト：FOCUS_MSは preposition=9000/その他=12000、SCROLL_MAX=48。env名は`dual_line_live_executor.py` 2693/2764/2776 で確認）

### 実測（bafather v4・7分）
| | LOOP-SLOW | 賭け |
|---|---|---|
| キャップ無(ベース) | avg 8.7s / **max 18〜138s** | 詰まる・取りこぼし |
| **+キャップ** | avg 4.3s / **max 7.9s** | bet/settle 16件・正常 |

→ **最悪138秒が7.9秒に（94%減）・平均半減・賭け正常**。ユーザー体感「問題なさそう」。

## v3への影響＝有益or無害（マイナス無し）
envはモード非依存（エンジン全体）。v3はNOW稀＝センタリング稀＝キャップが効く場面ほぼ無し。速いセンタリング(v3大多数)は成功後すぐ返るのでキャップ無関係。稀に重い時だけ光り優先＝**v3に保険が付くだけ**。→ **v3・v4両方つけっぱなしでOK**。

## デプロイ方針
- bafather：設定済・稼働中（**数日様子見**）。
- 受け子標準化：`copytrade_gui/build_staging/.env` に4行追加（provisionが既存キー温存→次回ビルドで反映）。※build_stagingは未追跡なので本MDが正本。
- **ライブ受け子への展開はbafather様子見後**（env追加だけ＝安全・各受け子.envに4行 or 再ビルド配布）。

## さらに詰めるなら（任意・未実施）
- キャップをさらに下げる（FOCUS_MS=800等）→ 1回をさらに短く。
- 残りの主因 `_ensure_multi_area`（WS復旧リロード・稀に38〜138s）を別途抑制（`BACOPY_MULTI_DEADLOCK_RELOAD_*`）。
- 精密版＝「決済が待っている時はその場のセンタリングを即スキップ」（小コード・後日）。

## 教訓
「重いものを最適化」より**「不要な見た目処理に時間予算を付けて、本業(決済/光り)を優先」**が筋。WS賭けがDOM非依存なので成立。measure-firstで①の空振りも捕捉。env先行＝最小リスクで実用域に到達。
